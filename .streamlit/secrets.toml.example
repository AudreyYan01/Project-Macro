# Copy this file to .streamlit/secrets.toml and fill in your key.
# secrets.toml is gitignored — never commit it.
# On Streamlit Cloud, set FRED_API_KEY in the app's Secrets settings instead.

FRED_API_KEY = "your_fred_api_key_here"
